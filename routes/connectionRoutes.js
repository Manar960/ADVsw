// const express = require('express');
// const router = express.Router();


// router.post('/', createConnection);//Establish a connection with another user who shares similar concerns or locations.
// router.get('/:connectionId', getConnection);//Retrieve information about a specific connection.
// router.delete('/:connectionId', deleteConnection);

// module.exports = router;
